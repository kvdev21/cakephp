/*requirejs.config({
    baseUrl: "js/vendor",
    paths: {
        view: "../view",
        lib: "../lib"
    },
    priority: ["order!lib/underscore"]
});*/

// Start the main app logic.
/*requirejs(["jquery", "underscore", "backbone"],
    function ($, _, backbone) {
        //jQuery, underscore and backbone are all
        //loaded and can be used here now.

        console.log("hi");
    });*/